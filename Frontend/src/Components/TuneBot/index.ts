// src/Frontend/src/components/TuneBot/index.ts
// Export the main TuneBot component so that other parts of the app can import it
// using a short path (`../Components/TuneBot`) instead of the full file name.
export { default } from "./TuneBot";
